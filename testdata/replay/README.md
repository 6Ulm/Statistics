Phase 3. Recorded telemetry replayed through the candidate engine with frozen expected
outputs (§33.1), plus out-of-order, duplicate, dropped, delayed and discontinuous sample
cases, and permission/location/provider state-machine sequences. Replay data MUST preserve
original timestamps and frames (§23). See `../README.md`.
