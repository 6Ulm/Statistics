Phase 1. §21.1 golden fixtures — per sector: centre, both boundaries, boundaries `± epsilon`,
`± 0.1°`, `± 1.0°`, plus the north-wrap sector. §21.4 straddle sets including every boundary
from both sides, exact equality, wide bounds, north wrap and the full-circle degenerate case.
§21.2 cases enumerating both hidden Google reference hypotheses, positive and negative
declination, and TRUE/MAGNETIC rulesets without dropping the ambiguity term.
See `../README.md`.
