Phase 1. §9 / §9.1 cross-runtime angle fixtures — normalization, the `(-180,180]` antipode in
both orderings of at least two distinct pairs, `359/1`, negatives, `360 → 0`, nonfinite, very
large finite inputs, and pinned quantile/median parity across Android, iOS and `analysis/`.
See `../README.md`.
