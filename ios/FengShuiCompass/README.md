# `FengShuiCompass` app target

SPEC.md §4.1. The SwiftUI app target's sources live here. Phase 0 establishes the boundary
only: the Precision screen, target guidance, calibration UI, Sensor Health and the §29.7
benchmark screen are Phase 4 work, and the Core Location / Core Motion adapters they consume
are Phase 2 work in `HeadingApple`.

Nothing is stubbed here. §37: a deliberate unsupported state with a reason beats a fake value.
