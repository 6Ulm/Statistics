// SPEC.md §4.1: "Diagnostic modules never become the production estimator."
// This target will own the iOS raw magnetometer/motion diagnostic streams feeding the §16
// interference features and the §15.1 cross-validation pipeline set. Phase 2/3 work; the
// boundary exists now so nothing else can grow into it.
